/**
 * Drawer · Strata (Sprint 04)
 * Nome canônico da biblioteca enterprise para o Sheet (Radix).
 * Mesmo componente — elevação nível 4 (shadow-overlay + z-overlay).
 */
export {
  Sheet as Drawer,
  SheetTrigger as DrawerTrigger,
  SheetClose as DrawerClose,
  SheetContent as DrawerContent,
  SheetHeader as DrawerHeader,
  SheetTitle as DrawerTitle,
  SheetBody as DrawerBody,
  SheetFooter as DrawerFooter,
} from "./sheet";
