/** @modules/presentation — visibilidade de blocos para apresentação (M-PRES). */
export * from "./types";
export { PresentationProvider, useVisibility } from "./context";
export { BlockMenu, HiddenBlocksNotice } from "./components";
